const styles = {
  variant: "flat", // flat | gradient | outline
  axisLine: {
    stroke: "var(--neutral-alpha-weak)",
  },
  tick: {
    fill: "var(--neutral-on-background-weak)",
    fontSize: 11,
  },
  tickLine: false,
};

export { styles };